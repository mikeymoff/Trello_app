<%= form_with(model: @board, local: true) do |form| %>
  
    
  <%= form_for @board do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>

    <div class="actions">
    <%= form.submit %>
  </div>
  <% end %>

  
<% end %>
