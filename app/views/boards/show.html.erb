<p id="notice"><%= notice %></p>

 
<% @board %>
<h1><%= @board.title %></h1>
 <%= link_to 'Edit', edit_board_path(@board)%>
  <%= link_to 'Delete', @board, method: :delete %>
  <%= link_to 'Back', boards_path %>
  
 <table class="striped">
  <thead>
    <tr>
      <th>List Title</th>
      <th> </th>
      <th>Edit List</th>
      <th>Delete List</th>
    </tr>
  </thead>
  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= link_to list.text, board_list_path(list) %><td>
        <td><%= link_to 'Edit', edit_board_list_path(list)%></td>
        <td><%= link_to 'Delete', board_list_path(list), method: :delete %></td>
      </tr>
    <% end %>
   
  </tbody>  
<table>  



